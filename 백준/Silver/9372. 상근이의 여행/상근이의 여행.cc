#include <iostream>
using namespace std;

int main(){
   int T;
    cin >> T;
    int n, m;
    for(int tc = 1; tc <= T; tc++){
        cin >> n >> m;
        for(int i = 0; i < m; i++){
            int a, b;
            cin >> a >> b;
        }
        
        cout << n-1 << "\n";
    }
}